# Config / Projection / Recovery Fallback Residual Disposition

Step: `invar_guard_remediation.fix-config-projections-recovery-budget-closure`

This register is intentionally limited to the owned files in this slice.
It does not claim closure for `orch_app.py`, `driver.py`, `inspection_queries.py`,
or project-level guard debt outside this step boundary.

| file | requirement_ref | behavior_claim | runtime_proof_expected | evidence_ref | status | closure_path | gate_decision_basis |
|---|---|---|---|---|---|---|---|
| `src/vectl/orchestration/config.py` | `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 41-43; `INVAR.md` Core/Shell rules | Config loader/default factory/snapshot exception behavior and precedence remain public compatibility surfaces; true Shell boundaries still read env/files/subprocess or preserve direct-return public APIs. | Config explicit-path load, role override/custom/default precedence, env override, snapshot round trip, validation diagnostics. | `uvx invar-tools guard --all` before/after owned-file budget excerpts; targeted `test_role_profile_overrides.py` and config-loading tests. | `DISPOSITIONED_RESIDUAL` | `STRUCTURED_RESIDUAL_DISPOSITION`: existing Core extraction modules are present, but the shell compatibility module still exceeds per-file escape budget until public API migration or further adapter split is authorized. | No broad suppression or budget lowering was added; residual is owner-visible and does not assert full-guard green while non-owned project debt remains. |
| `src/vectl/orchestration/projections.py` | `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 44-46; `tests/orchestration/unit/test_projections_impl.py` | Projection artifact schemas (`state/latest.json`, `state/summary.json`, `state/metrics.json`), replay cursor behavior, stale diagnostics, and drive projection compatibility remain public inspection/recovery surfaces. | Replay fixture writes identical latest/summary/metrics, step/case artifact hooks remain populated, persistence conflict raises `PROJECTION_STALE`. | `uvx invar-tools guard --all` before/after owned-file budget excerpts; targeted projection tests. | `DISPOSITIONED_RESIDUAL` | `STRUCTURED_RESIDUAL_DISPOSITION`: pure reducer/payload Core extraction exists, but shell projection adapters still require direct DTO returns and persisted artifact exception behavior. | Residual is explicitly tied to artifact schema compatibility and stale diagnostics; no new broad allow was introduced. |
| `src/vectl/orchestration/recovery_fallback.py` | `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 61-63; `docs/RFC-opencode-orchestration-runner.md` sections 10-12 | Native-session-resume vs fresh-relaunch truth labels, missing-file fallback, legacy `None` sentinel readback, and recovery artifact paths remain compatibility behavior. | Session validation success and failure reasons; native resume, fresh relaunch, explicit failure; continuity/attempt persistence and malformed readback. | `uvx invar-tools guard --all` before/after owned-file budget excerpts; targeted recovery fallback integration tests. | `DISPOSITIONED_RESIDUAL` | `STRUCTURED_RESIDUAL_DISPOSITION`: pure decision Core helper exists, while file-reading/writing public helpers retain direct-return compatibility. | Residual preserves documented recovery audit semantics; no broad suppression or Core/Shell drift was introduced. |
