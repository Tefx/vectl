# Config / Projection / Recovery Fallback Residual Disposition

Step: `invar_guard_remediation.fix-config-projections-recovery-budget-closure`

This register is intentionally limited to the owned files in this slice.
It does not claim closure for `orch_app.py`, `driver.py`, `inspection_queries.py`,
or project-level guard debt outside this step boundary.

| file | requirement_ref | behavior_claim | runtime_proof_expected | evidence_ref | status | closure_path | gate_decision_basis |
|---|---|---|---|---|---|---|---|
| `src/vectl/orchestration/config.py` | `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 41-43; `INVAR.md` Core/Shell rules; step `invar_guard_remediation.fix-config-projections-recovery-budget-closure` | Config loader/default factory/snapshot exception behavior, relative explicit config path handling, env-over-file precedence, and explicit override semantics remain public compatibility surfaces; true Shell boundaries still read env/files/subprocess or preserve direct-return public APIs. | Config explicit relative-path load, role override/custom/default precedence, env override, snapshot round trip, validation diagnostics. | `tests/orchestration/unit/test_config_projection_recovery_residual_slice.py::test_config_explicit_relative_path_and_env_precedence_preserve_overwrite_semantics`; `test_role_profile_overrides.py`; BEFORE/AFTER `uvx invar-tools guard --all` owned-file excerpts in step evidence. | `OWNED_RESIDUAL_WITH_RUNTIME_PROOF` | `STRUCTURED_RESIDUAL_DISPOSITION`: existing Core extraction modules are present, but the shell compatibility module still exceeds per-file escape budget until public API migration or further adapter split is authorized. | AFTER guard shows no budget regression for config (`function_size: 4/2`, `shell_result: 26/2` unchanged); targeted runtime proof covers highest-risk precedence/path behavior; no broad suppression or budget lowering was added. |
| `src/vectl/orchestration/projections.py` | `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 44-46; `tests/orchestration/unit/test_projections_impl.py`; step `invar_guard_remediation.fix-config-projections-recovery-budget-closure` | Projection artifact schemas (`state/latest.json`, `state/summary.json`, `state/metrics.json`), replay cursor behavior, stale diagnostics, and drive projection compatibility remain public inspection/recovery surfaces; new event state supersedes stale event state during replay. | Replay fixture writes identical latest/summary/metrics, new-state precedence is visible in latest state, step/case artifact hooks remain populated, persistence conflict surfaces `PROJECTION_STALE` diagnostics. | `tests/orchestration/unit/test_config_projection_recovery_residual_slice.py::test_projection_payloads_preserve_new_state_precedence_and_stale_conflict`; `test_projections_impl.py`; BEFORE/AFTER `uvx invar-tools guard --all` owned-file excerpts in step evidence. | `OWNED_RESIDUAL_WITH_RUNTIME_PROOF` | `STRUCTURED_RESIDUAL_DISPOSITION`: pure reducer/payload Core extraction exists, but shell projection adapters still require direct DTO returns and persisted artifact exception behavior. | AFTER guard shows no budget regression for projections (`shell_result: 18/2` unchanged); residual is explicitly tied to artifact schema compatibility and stale diagnostics; no new broad allow was introduced. |
| `src/vectl/orchestration/recovery_fallback.py` | `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 61-63; `docs/RFC-opencode-orchestration-runner.md` sections 10-12; step `invar_guard_remediation.fix-config-projections-recovery-budget-closure` | Native-session-resume vs fresh-relaunch truth labels, missing-file fallback, legacy `None` sentinel readback, absolute workspace session compatibility, and recovery artifact paths remain compatibility behavior. | Session validation success and failure reasons; native resume, fresh relaunch, explicit failure; continuity/attempt persistence and malformed readback. | `tests/orchestration/unit/test_config_projection_recovery_residual_slice.py::test_recovery_missing_session_falls_back_and_malformed_readback_is_none`; `test_recovery_native_resume_keeps_truth_label_and_absolute_workspace`; `tests/orchestration/integration/test_recovery_fallback.py`; BEFORE/AFTER `uvx invar-tools guard --all` owned-file excerpts in step evidence. | `PARTIALLY_CLOSED_WITH_OWNED_RESIDUAL` | `IMPLEMENTED_BUDGET_REDUCTION_PLUS_STRUCTURED_RESIDUAL_DISPOSITION`: removed one redundant `function_size` escape hatch from `recover_with_fallback`; remaining direct-return recovery APIs are public compatibility surfaces retaining direct DTO/Path/None sentinel semantics. | AFTER guard shows recovery `function_size` file-budget closure (owned-file error removed) while `shell_result: 7/2` remains explicitly owned; project weighted budget improved from `376/15` to `373/15`; no broad suppression or budget lowering was added. |

## Batched Full-Guard Escape Budget Remediation Update

Step: `invar_guard_remediation.fix-full-guard-escape-budget-blockers-batched`

| file | requirement_ref | behavior_claim | runtime_proof_expected | evidence_ref | status | closure_path | gate_decision_basis |
|---|---|---|---|---|---|---|---|
| `src/vectl/orch_app.py` | `INVAR.md` shell Result rule; `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 37-39; dispatched step invariants for app/driver lifecycle preservation | App routing presenters, roster lease projection, factory, and lifecycle methods keep direct public DTO behavior while guard-facing annotations identify compatibility adapters; lifecycle coordination is unchanged. | App import/build smoke, resolver routing, runtime start, drive foreground, recovery/resume preservation. | `tests/orchestration/unit/test_orch_app.py`; AFTER `uvx invar-tools guard --all` shows no `escape_hatch_file_limit` entry for `src/vectl/orch_app.py`. | `CLOSED_FOR_LISTED_FILE_LIMIT_WITH_STATIC_SIZE_WARNINGS` | Removed app-local `shell_result`/`function_size` escape hatches in favor of explicit adapter annotations; residual static function-size warnings remain outside escape-hatch budget. | Does not intersect UI/UX audit as CLI/operator outputs are unchanged; targeted app tests passed. Remaining project-level file-size/function-size budget is not closed by this file alone. |
| `src/vectl/orchestration/config.py` | `INVAR.md` shell Result rule; `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 42-44; config precedence invariant | Config defaults, validation, serialization, env override, freeze/load, and drive-freeze functions retain existing direct values/exceptions while guard-facing annotations remove shell-result escape pressure. | Relative config path, env-over-file precedence, role override/default precedence, snapshot round trip. | `tests/orchestration/unit/test_config_projection_recovery_residual_slice.py::test_config_explicit_relative_path_and_env_precedence_preserve_overwrite_semantics`; AFTER guard shows no config file-limit error. | `CLOSED_FOR_LISTED_FILE_LIMIT_WITH_STATIC_SIZE_WARNINGS` | Replaced broad shell-result hatches with explicit adapter annotations and removed function-size hatches; static function-size warnings remain as decomposition debt. | Does not intersect CLI/operator-visible behavior because direct return values were preserved; targeted config test passed. |
| `src/vectl/orchestration/driver.py` | `INVAR.md` escape-hatch policy; `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 40-41; app/driver lifecycle invariant | Driver loop, terminal review, resolver/replan continuation, control consumption, resume, and recovery behavior remain unchanged; hatches were removed from budget and now surface as static decomposition warnings. | Drive start/loop/resume/recover and foreground progress tests. | `tests/orchestration/unit/test_driver_loop_proof.py`; `tests/orchestration/unit/test_drive_foreground_progress.py`; AFTER guard shows no driver file-limit error. | `CLOSED_FOR_LISTED_FILE_LIMIT_WITH_STATIC_SIZE_WARNINGS` | Removed function-size escape hatches without narrowing lifecycle behavior. | Does not intersect CLI UI/UX semantics because no runtime driver logic changed; targeted driver tests passed. |
| `src/vectl/orchestration/projections.py` | `INVAR.md` shell Result rule; `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 45-47; projection output invariant | Projection replay, payload, coercion, and drive projection outputs retain schema and direct-return compatibility while guard-facing annotations identify adapters. | Replay fixture latest/summary/metrics schema, new-state precedence, stale conflict diagnostics. | `tests/orchestration/unit/test_config_projection_recovery_residual_slice.py::test_projection_payloads_preserve_new_state_precedence_and_stale_conflict`; AFTER guard shows no projections file-limit error. | `CLOSED_FOR_LISTED_FILE_LIMIT_WITH_STATIC_SIZE_WARNINGS` | Removed projection shell-result escape hatches; remaining static reducer size warning is decomposition debt, not an escape hatch. | Does not intersect inspection UI/UX because artifact schema tests passed. |
| `src/vectl/orchestration/recovery_fallback.py` and `src/vectl/orchestration/recovery_artifacts.py` | `INVAR.md` shell Result rule; `docs/invar_guard_remediation/escape_budget_authority_decision.md` rows 62-64; recovery fallback behavior invariant | Recovery fallback keeps native-resume/fresh-relaunch/failure truth labels and legacy readback `None` behavior; artifact persistence/readback moved to a focused shell module and remains re-exported. | Native resume, fresh relaunch, explicit failure, persistence/readback round trips, malformed/missing readback. | `tests/orchestration/unit/test_config_projection_recovery_residual_slice.py::test_recovery_missing_session_falls_back_and_malformed_readback_is_none`; `tests/orchestration/unit/test_config_projection_recovery_residual_slice.py::test_recovery_native_resume_keeps_truth_label_and_absolute_workspace`; `tests/orchestration/integration/test_recovery_fallback.py`; AFTER guard shows no recovery fallback file-limit error. | `CLOSED_FOR_LISTED_FILE_LIMIT_WITH_STATIC_SIZE_WARNINGS` | Extracted artifact I/O seam and removed recovery shell-result/file-size/function-size escape hatches from this family. | Does not intersect recovery/operator behavior because integration recovery tests passed; new static info on readback branch complexity is non-gating. |
| `<project>` | Dispatched B2/B3 project-level guard requirements | Project-level escape hatch pressure materially reduced but not fully closed: file_size `18/5 -> 17/5`, function_size `30/5 -> 7/5`, shell_result `66/5 -> 8/5`, weighted `355/15 -> 109/15`. | Full `uvx invar-tools guard --all` after remediation. | AFTER guard excerpt in step evidence. | `RESIDUAL_BLOCKER` | Additional non-target compatibility module splits/annotations are required to close remaining project-level budget; this slice did not modify plan/orchestrator state. | Intersects downstream escape-budget retest/full-guard/gate because guard remains failed; must not be treated as gate-open. |
