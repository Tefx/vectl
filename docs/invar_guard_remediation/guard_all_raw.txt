Stale raw guard output retired during B2 forbidden-residue cleanup.

The prior captured log named historical implementation paths and was no longer authoritative. Re-run the current guard command when fresh full-scan evidence is required.
