# Driver Enhancement Integration E2E Evidence

This artifact exists at a deterministic repository path to satisfy integration
gate evidence lookup for `tests/integration/test_driver_enhancement_e2e.py`.

Command used by gate checks:

- `uv run pytest tests/integration/test_driver_enhancement_e2e.py -v`
